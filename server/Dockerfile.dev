# Development Dockerfile — hot reload with ts-node-dev
FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cache layer)
COPY package.json package-lock.json* ./
RUN npm ci

# Copy source
COPY . .

EXPOSE 5000

CMD ["npm", "run", "dev"]
